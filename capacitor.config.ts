import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sveltekitapp',
  appName: 'ß╗Éi dß╗ôi ├┤i',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
